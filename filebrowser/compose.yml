services:
  filebrowser:
    image: filebrowser/filebrowser:v2.55.0
    restart: unless-stopped
    user: 1000:1000
    volumes:
      - ./media:/srv
      - /home/chris/Documents/Sync:/srv/Sync
      - ./data:/database
      - ./config:/config
    ports:
      - 127.0.0.1:3070:80
